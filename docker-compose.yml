version: '3'
services:
  frontend:
    ports:
      - "80:80"
    build:
      context: ./
      dockerfile: Dockerfile
  backend:
    ports:
      - "11088:11088"
    build:
      context: ./
      dockerfile: Dockerfile
    command: "node backend/server.js"
