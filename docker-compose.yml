version: '3'
services:
  frontend:
    ports:
      - "80:80"
    build:
      context: ./
      dockerfile: Dockerfile.frontend
  backend:
    ports:
      - "11088:11088"
    build:
      context: ./
      dockerfile: Dockerfile.frontend
    command: "node backend/server.js"
